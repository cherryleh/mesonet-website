import { Component } from '@angular/core';

@Component({
  selector: 'app-about',
  standalone: true,
  templateUrl: './about.component.html',  // Reference to the separate HTML file
  styleUrls: ['./about.component.css']    // Reference to the separate CSS file
})
export class AboutComponent {}
